# QuestBoard server environment.
# Copy to server/.env and adjust as needed.

# SQLite for local dev; swap for a Postgres URL in production.
DATABASE_URL="file:./prisma/dev.db"

# Port the metadata API listens on.
PORT=4000
